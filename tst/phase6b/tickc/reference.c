double reference_mixed(long first, double a, float b, double c, float d,
		       double e, float f, double g, float h, double i,
		       float j, long last)
{
	return first + a + b + c + d + e + f + g + h + i + j + last;
}
